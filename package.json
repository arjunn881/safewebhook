{
  "name": "safewebhook",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro build && wrangler dev --port 3000",
    "dev:astro": "astro dev --port 3000",
    "dev:cf": "astro build && wrangler dev --port 3000",
    "build": "astro build",
    "check": "astro check",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.2",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@cloudflare/workers-types": "^5.20260818.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.129.0"
  }
}
