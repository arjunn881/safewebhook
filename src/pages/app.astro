---
import Layout from '../layouts/Layout.astro';
import WebhookDashboard from '../components/WebhookDashboard.astro';

export const prerender = false;
---

<Layout
  title="Online Webhook Tester Workspace — Live Payload Inspector | SafeWebhook"
  description="Real-time edge webhook debugging workspace. Intercept HTTP requests, inspect full headers and payloads with zero configuration at safewebhook.com."
  keywords="online webhook tester app, webhook workbench, inspect webhooks live, real-time webhook debugger workspace, free request logger tool"
>
  <WebhookDashboard />
</Layout>
